name: 04 - using actions

on:
  push

jobs: 
  build:
    runs-on: ubuntu latest

    steps: 
      - name: checkout code
        uses: actions/checkout@v3

      - name: printing folders
        run: ls -R

